{
  "name": "Volunteer Portal Mock App",
  "description": "A barebones Rails app to test end to end implementation",
  "repository": "https://github.com/adam-hart/dummy_app",
  "logo": "https://emojipedia-us.s3.amazonaws.com/thumbs/240/apple/129/fox-face_1f98a.png",
  "keywords": ["rails", "dummmy"],
  "addons": [
    "cleardb:ignite"
  ],
  "scripts": {
    "postdeploy": [
      "heroku config:set DATABASE_URL=DB_URL=\"$(heroku config | grep CLEARDB_DATABASE_URL | sed -n '/CLEARDB_DATABASE_URL/s/  */ /gp' | cut -d ' ' -f 2 | sed -e 's/mysql:/mysql2:/g')\"",
      "bundle exec rake db:migrate"
    ]
  }
}
